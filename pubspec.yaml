name: pbl_sertifikasi
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  dotted_border: ^2.0.0
  flutter_dash: ^1.0.0
  http: ^1.0.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  file_picker: ^8.1.4
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  universal_html: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_polinema.png
    - assets/List.png
    - assets/sertifikat.png
